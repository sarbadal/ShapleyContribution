# encoding=utf-8

import os

# Do not change the following line code
BASE_DIR = os.path.normpath(
    os.path.dirname(os.path.realpath(__file__)) + os.sep + os.pardir
)
DATA = os.path.join(BASE_DIR, 'data')
